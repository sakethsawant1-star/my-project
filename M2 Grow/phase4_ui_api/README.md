# Phase 4: User Interface & Deployment

This directory will contain the application interface and API endpoints:
- A lightweight REST API (FastAPI/Flask) wrapping the complete pipeline (Guardrails -> Retrieval -> Generation).
- A minimal UI frontend (Streamlit/React) to interact with the API, showing the welcome message, example questions, and disclaimer.
